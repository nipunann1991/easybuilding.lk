import { RoundToDecimalPipe } from './round-to-decimal.pipe';

describe('RoundToDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundToDecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
